{% extends 'base.html.twig' %}

{% block title %}Welcome to our events
{% endblock %}

{% block body %}
	{% if app.user %}
		<div class="bg-dark text-center text-white p-3">
			Back to your <a href={{ path('admin') }} class="btn btn-light btn-sm rounded-pill text-dark">dashboard</a>
		</div>
	{% endif %}
	<div class="container">
		<div class="">
			{{ include('components/header.html.twig') }}
		</div>
		<div class="">
			{{ include('components/logos.html.twig') }}
		</div>
		<div class="d-flex overflow-x-scroll">
			{{ include('components/event-card.html.twig') }}
		</div>
		<div class="">
			{{ include('components/footer.html.twig') }}
		</div>
	</div>
{% endblock %}
